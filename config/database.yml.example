#
# Database
#

# we wil have redis and mongo connections, but the keys will be different.
# right now it's just a stub, key names would be someting like
# "documents_backend" (by the nature of DB usage)

^development:

  database:
    redis:
      host: localhost
      port: 6379

    mongo:
      host: localhost
      port: 27017
      database: nodeca

^production:

  database:
    redis:
      host: localhost
      port: 6379
      # db index, not working for clusters
      #index: 0

    mongo:
      host: localhost
      port: 27017
      database: nodeca
      #user: mongouser
      #password: m0NGoP4$$WORd
